This is a simple application for sending Softel Newfor teletext data to an endpoint.

The command line options are --ip for the destination IP address and --port for the destination port number.
--page to specify the teletext page number, default is 888
